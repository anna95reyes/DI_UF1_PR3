<UserControl
    x:Class="NBA.View.UIPlayOffEquipFinals"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NBA.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock x:Name="txbNomEquip" Text="San Antonio Spurs"/>
            <TextBlock x:Name="txbPosicioEquip" Margin="5 0 0 0" Text="(1)"/>
        </StackPanel>

        <Image Grid.Row="1" Height="100" VerticalAlignment="Center"
               HorizontalAlignment="Center">
            <Image.Source>
                <SvgImageSource UriSource="/Assets/nba_images/teams/SAS.svg"
                                RasterizePixelHeight="400" RasterizePixelWidth="400"/>
            </Image.Source>
        </Image>

        <Border x:Name="brdEquip" Grid.Row="2" Background="#FFEA9999" >
            <TextBlock x:Name="txbPuntuacioEquip" Text="4" HorizontalAlignment="Center"
                       VerticalAlignment="Center" Foreground="White"/>
        </Border>
    </Grid>
</UserControl>
